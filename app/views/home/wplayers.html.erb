<h1>Player Weekly Ranks</h1>

<div class="container">
	<div class="row">
		<div class="col">
			<h2>Batting last 9 days</h2>
			<ol>
				<% wbplayers.each do |k,v| %>
					<li class="text-white"><h5><%= k %> : <small><%= v %></small></h5></li>
				<% end %>
			<ol>
		</div>
		<div class="col">
			<h2>Pitching 9 day</h2>
			<ol>
				<% wbpitching.each do |k,v| %>
					<li class="text-white"><h5><%= k %> : <small><%= v %></small></h5></li>
				<% end %>
			<ol>
		</div>
	</div>
</div>